# Internal eval sample data (committed)

Fake/sanitized artifacts for the password-protected **`/internal/eval`** viewer on deployed builds.

- No partner exports or real customer data
- `example.com` / `example.org` URLs only
- Safe to deploy; not used by `/dev/eval` (local gitignored outputs)

Future: `/internal/eval` may read from private object storage instead of this folder.
